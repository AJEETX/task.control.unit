@model risk.control.system.Models.BeneficiaryRelation
@{
    ViewData["Title"] = @Applicationsettings.CREATE;
    Layout = "/Views/Shared/AdminLTE/_FormLayout.cshtml";
}
<section class="content pt-2">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-secondary">
                    <div class="card-header">
                        <h3 class="card-title"><i class="fas fa-user-tie"></i> @Applicationsettings.CREATE  @Applicationsettings.BENEFICIARY</h3>
                    </div>
                    <div class="card-body">
                        <form id="create-form" asp-action="Create">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                            <div class="form-group">
                                <label asp-for="Name" class="control-label"></label>
                                <input asp-for="Name" class="form-control" type="text" placeholder="Enter Name" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="Code" class="control-label"></label>
                                <input asp-for="Code" class="form-control" type="text" maxlength="5"/>
                                <span asp-validation-for="Code" class="text-danger"></span>
                            </div>
                            <div class="form-group text-right">
                                <a asp-action="Index" class="btn btn-secondary"><i class="fa fa-window-close"></i> @Applicationsettings.CANCEL</a> &nbsp;
                                <button type="submit" class="btn btn-success"> <i class="fas fa-user-tie"></i> @Applicationsettings.CREATE @Applicationsettings.BENEFICIARY</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function () {

            var askConfirmation = true;
            $('#create-form').submit(function (e) {
                if (askConfirmation) {
                    e.preventDefault();
                    $.confirm({
                        title: "Confirm Create",
                        columnClass: 'medium',
                        content: "Are you sure to create?",
                        icon: 'fas fa-user-tie',
                        type: 'green',
                        closeIcon: true,
                        buttons: {
                            confirm: {
                                text: "Create item",
                                btnClass: 'btn-success',
                                action: function () {
                                    askConfirmation = false;
                                    $('#create-form').submit();
                                }
                            },
                            cancel: {
                                text: "Cancel",
                                btnClass: 'btn-default'
                            }
                        }
                    });
                }
            })
        });
    </script>
}
